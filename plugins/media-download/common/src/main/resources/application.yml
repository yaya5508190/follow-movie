logging:
  level:
    root: debug
server:
  port: 8083

spring:
  application:
    name: plugin-loader
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/follow_movie
    username: postgres
    password: 123